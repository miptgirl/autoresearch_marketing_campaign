- If the objective later changes from "maximize revenue on this fixed dataset" to "solve many larger campaign tables quickly", benchmark exact-solver runtime directly and compare the current branch-and-bound approach against Pareto-frontier dynamic programming.
- If even richer business constraints are introduced later (per-country limits, channel caps, mandatory inclusions/exclusions, minimum regional coverage), keep the MILP formulation and extend the linear constraints rather than piling more custom pruning logic onto the fallback search.
- If portability becomes a problem because SciPy is unavailable in some environments, benchmark a stronger pure-Python fallback: either a Pareto-frontier DP on `(spend, cs_contacts, rate_excess)` or a branch-and-bound ordered by Lagrangian scores with tighter multi-constraint bounds.
